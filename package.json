{
  "name": "citadel-onchain",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js",
    "deploy:alfajores": "hardhat run --network alfajores scripts/deploy.js",
    "deploy:celo": "hardhat run --network celo scripts/deploy.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.6",
    "@nomiclabs/hardhat-etherscan": "^3.1.8",
    "hardhat": "^2.22.6"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.10.0"
  }
}
